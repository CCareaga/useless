STOR 96 A

~ loop though characters until we get to 'd'
~ then hop to mutate, in mutate we change the SUB instruction to SUB
~ and we change the first instruction after mutate into EXIT so that
~ on the second run of the loop we decrement and then exit the program

$loop
    ADD 1 A
    PRINT A
    EQ 100 A
    HOPT mutate
    HOP loop

$mutate
    PRINT newline
    STOR 7 loop
    STOR 104 A
    STOR 98 mutate
    HOP loop

$newline 10
